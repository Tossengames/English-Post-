name: test-trigger

on:
  workflow_dispatch:
    inputs:
      test_input:
        description: 'Type something here'
        default: 'Hello World'

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "The input was ${{ github.event.inputs.test_input }}"
